{
  "name": "wallet-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Ethereum wallet monitor — run with or without building the frontend",
  "scripts": {
    "postinstall": "node scripts/install-all.js",
    "build": "npm run build --prefix frontend",
    "dev": "concurrently \"cross-env PROXY_TO_VITE=1 npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npm run build && npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">=18"
  }
}
