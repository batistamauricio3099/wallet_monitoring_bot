PORT=8001
# Set your own MongoDB URI (never commit real values)
MONGODB_URI=
FETCH_INTERVAL_MINUTES=30
# Ethereum refresh (ms)
ETH_REFRESH_INTERVAL_MS=6000
CHAIN_DELAY_MS=400
ETHERSCAN_BASE=https://etherscan.io
# Set a strong admin password (never commit real values)
ADMIN_PASSWORD=
